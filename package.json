{
  "name": "birthday-card-system",
  "version": "7.1.0",
  "private": true,
  "description": "工会生日彩信和电子贺卡系统",
  "scripts": {
    "install:all": "cd server && npm install && cd ../admin-web && npm install && cd ../server/monitor && npm install",
    "dev:server": "cd server && node src/app.js",
    "dev:frontend": "cd admin-web && npm run dev",
    "build:frontend": "cd admin-web && npm run build"
  },
  "engines": {
    "node": ">=18.11.0"
  }
}
